meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: http://localhost:4000/api/login
  body: json
}

body:json {
  {
    "username": "reader",
    "password": "library123"
  }
}

script:post-response {
  if (res.body.token) {
    bru.setEnvVar("token", res.body.token);
  }
}
